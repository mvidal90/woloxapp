import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import { WoloxApp } from './components/WoloxApp';

ReactDOM.render(
  <React.StrictMode>
    <WoloxApp />
  </React.StrictMode>,
  document.getElementById('root')
);
